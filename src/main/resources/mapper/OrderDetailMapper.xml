<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.OrderDetailMapper">
    <insert id="insertOrderDetail" parameterType="map">
        INSERT INTO order_detail (order_id, item_id, num, name, spec, price, image)
        VALUES
        <foreach collection="carts" item="cart" separator=",">
            (#{order_id}, #{cart.item_id}, #{cart.num}, #{cart.name}, #{cart.spec}, #{cart.price}, #{cart.image})
        </foreach>
    </insert>
</mapper>